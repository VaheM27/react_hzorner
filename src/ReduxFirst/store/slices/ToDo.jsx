const initialState = {
    todoName: "",
    todoList: []
}


const ToDo = (state = initialState, action) => {
    switch (action.type) {
        case value:

            break;

        default:
    }

}

export default ToDo